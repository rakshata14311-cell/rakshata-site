import { defineConfig } from 'astro/config';

// Site URL will be updated to https://rakshitamulimani.com once the custom
// domain is connected. Until then this can point at whatever free host
// (Netlify/Vercel) issues.
export default defineConfig({
  site: 'https://rakshitamulimani.com',
});
